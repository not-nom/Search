import { useEffect, useState } from "react"

export default function HookTest({props}) {
  //const [menuIsOpened, setMenuIsOpened] = useState(false)
  //setMenuIsOpened((per) => !per)
  //useEffect(()=>{
  //
  //},[funct])

    return (
    <div>HookTest</div>
  )
}
